{
  "name": "@asta/shared-types",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "//": "Se publica el fuente TypeScript, no un build. Next.js lo transpila via transpilePackages y Metro (React Native) lo resuelve directo; asi no hay un paso de build que recordar antes de cada cambio.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "zod": "^4.0.0"
  }
}
